# Experiments with VScode extensions

## External Linter

Using an external command line executable to generates issues in a file being edited

```
cd externalLinter
npm install
npm run compile
```
Copy to $HOME/.vscode/extensions and open a new window to test
